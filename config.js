/* ============================================================
   ARCH design · studio — конфигурация сайта
   Единственный файл, который нужно править для подключения CRM.
   ============================================================ */
window.ARCH_CONFIG = {

  /* ---------- Куда уходят заявки ---------- */
  crm: {
    /* ВАРИАНТ 1 (рекомендуется). Свой эндпоинт-прокси — токен бота
       остаётся на сервере. Готовый код лежит в папке /server.
       Пример: 'https://arch-design.kz/api/lead.php'                      */
    endpoint: '',

    /* ВАРИАНТ 2 (быстрый старт, без сервера).
       Заявка уходит напрямую в Telegram Bot API из браузера.
       ВНИМАНИЕ: токен будет виден всем в исходниках страницы —
       используйте только для теста или для «одноразового» бота.       */
    telegram: {
      botToken: '',      // '7712345678:AAH...'  — выдаёт @BotFather
      chatId:   ''       // '-1001234567890' или ваш личный id (@userinfobot)
    },

    /* ВАРИАНТ 3 (всегда работает как запасной).
       Если ничего выше не заполнено — заявка открывается
       в WhatsApp менеджера уже готовым сообщением.                     */
    whatsapp: '77003030370'
  },

  /* ============================================================
     Аналитика. Без неё конверсию калькулятора измерить нечем.
     Счётчик подключается в <head>, сюда вписывается только номер.
     События, которые уже отправляются:
       lead_form · lead_calc · compare_used · cases_filter ·
       cases_more · click_phone · click_whatsapp · cta_* · instagram
     ============================================================ */
  analytics: {
    yandexMetrika: '',   // '98765432' — номер счётчика Яндекс.Метрики
    ga4:           '',   // 'G-XXXXXXXXXX' — Google Analytics 4
    debug:         false // true — печатать события в консоль при настройке
  },

  /* ---------- Контакты (используются в разметке и в заявках) ---------- */
  contacts: {
    phone:     '+7 700 303 03 70',
    phoneAlt:  '+7 778 008 77 88',
    email:     'arch_design.studio@gmail.com',
    instagram: 'arch_designstudio',
    city:      'Кызылорда'
  },

  /* ============================================================
     Калькулятор: вся ценовая модель в одном месте.
     Цены — тенге за 1 м², как в прайсе студии.
     ============================================================ */
  calc: {
    packages: {
      opt:     { label: 'Оптимальный',  min: 1000, max: 2000,  weeks: 2, includes: [] },
      classic: { label: 'Классический', min: 4000, max: 7000,  weeks: 4, includes: ['walls', 'viz'] },
      premium: { label: 'Премиум',      min: 8000, max: 15000, weeks: 6, includes: ['walls', 'viz', 'supervision'] }
    },

    /* Коэффициент сложности объекта */
    objects: {
      flat:   { label: 'Квартира',            k: 1.00, icon: 'i-flat'  },
      house:  { label: 'Частный дом',         k: 1.15, icon: 'i-house' },
      comm:   { label: 'Коммерция',           k: 1.25, icon: 'i-comm'  },
      land:   { label: 'Ландшафт / участок',  k: 0.85, icon: 'i-land'  }
    },

    /* Доп. опции: цена за 1 м² */
    options: {
      viz:         { label: '3D-визуализация всех помещений', price: 900,  weeks: 1 },
      walls:       { label: 'Развёртки стен и раскладка плитки', price: 600, weeks: 1 },
      supervision: { label: 'Авторский надзор на объекте',    price: 1100, weeks: 3 },
      facade:      { label: 'Дизайн экстерьера / фасад',      price: 1300, weeks: 2 },
      landscape:   { label: 'Благоустройство территории',     price: 700,  weeks: 2 },
      furniture:   { label: 'Индивидуальная мебель и узлы',   price: 800,  weeks: 1 }
    },

    /* Срочность */
    urgency: {
      normal: { label: 'Стандартный срок', k: 1.00, weeksK: 1.00 },
      fast:   { label: 'Ускоренно',        k: 1.20, weeksK: 0.70 }
    },

    /* Скидка за объём: от какой площади — какой множитель */
    volumeDiscount: [
      { from: 150, k: 0.95, label: 'скидка 5% за объём от 150 м²' },
      { from: 300, k: 0.90, label: 'скидка 10% за объём от 300 м²' },
      { from: 600, k: 0.85, label: 'скидка 15% за объём от 600 м²' }
    ],

    area: { min: 20, max: 1000, step: 5, def: 90 }
  }
};
